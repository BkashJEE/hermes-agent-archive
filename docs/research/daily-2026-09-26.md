# Daily source review — 2026-09-26

This proposal refreshes public measurements, not archive content. No entries were
removed, no plugin code was executed, and nothing was merged or deployed.

## Public measurements

The existing fetcher refreshed ten documented Hermes repositories from GitHub's
public API at `2026-09-26T18:09:42.123Z`. Each exceeds 50,000 stars. Previous measured
counts and timestamps provide the Trending baseline; no baseline was estimated.
All 85 stored repository records remain, including 75 older records marked stale.

The full-source fetch failed on Reddit HTTP 403 and left the previous file unchanged.
The fetcher's supported `--github-only` mode then completed. Hacker News and Reddit
snapshots were retained byte-for-byte at the JSON-value level, along with the existing
Reddit warning. They were not refreshed. Jev reclassified ten changed inputs and
reused 826 current assessments. The 840 stored shelf entries are unchanged.

## Bounded review

- [Skills System](https://hermes-agent.nousresearch.com/docs/user-guide/features/skills):
  checked Desktop browsing/install, blank-slate profiles, stacked skills and `/learn`.
  The existing importer dry run found 55 already-archived sections and no new entries.
- [Telegram](https://hermes-agent.nousresearch.com/docs/user-guide/messaging/telegram/):
  reviewed as official setup reference. Retained the manifest's deliberate exclusion
  of per-platform reference; no duplicate setup cards were added.
- [Cron documentation correction](https://github.com/NousResearch/hermes-agent/commit/f077152871798b8a666daf48333d123549d10672):
  Nous Research clarified recovery of invalid repeat counters. Deferred as maintenance
  reference, rather than a new workflow or an undocumented trick.
- [Session deletion clarification](https://github.com/NousResearch/hermes-agent/commit/66c5099b3140827fcd605dba238cae9950ead34a):
  Nous Research documents that deleting a live session does not stop its running chat;
  its next save recreates it. Useful limitation, but not a Hidden Trick. Deferred until
  a focused sessions import can preserve this context without importing the entire page.
- Rechecked upstream Hermes support for
  [Superpowers](https://github.com/obra/superpowers#hermes-agent),
  [CLI-Anything](https://github.com/HKUDS/CLI-Anything#-hermes) and
  [Unsloth](https://github.com/unslothai/unsloth).
  These are existing eligible projects, so no duplicate cards were added.
  CLI-Anything still labels its Hermes integration experimental/community; Superpowers
  requires restarting active sessions after installation.

Discovery returned 16 repository matches above the floor. Ten were already approved;
six remain excluded pending explicit upstream Hermes-support evidence:
`vinta/awesome-python`, `punkpeye/awesome-mcp-servers`, `stablyai/orca`,
`diegosouzapw/OmniRoute`, `1c7/chinese-independent-developer`, `zylon-ai/private-gpt`.
A search match alone does not establish support. No new candidate was admitted.

## Catalogue correction

The manifest said the Skills Hub had no public JSON endpoint. The official
[Skills System documentation](https://hermes-agent.nousresearch.com/docs/user-guide/features/skills/#browse-and-install-in-desktop)
now points to [the public catalogue](https://hermes-agent.nousresearch.com/docs/api/skills.json),
which returned HTTP 200 and valid JSON during this review. The manifest now records
that source without claiming this heading-based importer can ingest it. Catalogue
presence is not verification of every listed skill, author or repository.

## Validation

- `npm run check`: passed; 840 stored entries.
- `npm test`: 77 passed, including classification freshness.
- No UI changes; browser checks were not required.
- Repository remains public. Deployment/access settings were not changed.
